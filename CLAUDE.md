# MomPill 프로젝트 - AI 개발 규칙

## 프로젝트 정체성
- 이름: MomPill (가칭, 추후 변경 가능)
- 한 줄 정의: "엄마가 홈쇼핑에서 영양제 사기 전에 사진 찍으면 아들처럼 최저가와 성분을 알려주는 무료 앱"
- 타겟: 70대 어머니(사용 주체) + 40-50대 자녀(공유 대상)
- 수익 모델: 무료 + 쿠팡 파트너스 커미션

## 기술 스택 (고정, 변경 금지)
- Frontend: Flutter (FlutterFlow 하이브리드 가능)
- Backend: Supabase (DB + Auth + Edge Functions + Storage)
- AI: Gemini 2.0 Flash (OCR + 분석 통합)
- 커머스: 쿠팡 파트너스 (초기는 검색 URL + 트래킹ID, 추후 API)
- 인증: 카카오 로그인 단일
- 에러 추적: Sentry
- 분석: PostHog (free tier)

## 절대 규칙 (위반 시 작업 중단하고 사용자에게 확인)

1. **파일 생성 제한**: 한 세션에 신규 파일 3개 이상 만들지 말 것.
   기존 파일 수정으로 해결 가능하면 수정 우선.

2. **의도 먼저 명시**: 코드 작성 전 반드시 다음 3줄을 먼저 출력:
   - 목적:
   - 입력:
   - 출력:
   - 영향받는 파일:

3. **외부 API 호출 시 필수 포함**:
   - try-catch 래핑
   - Exponential backoff 3회 재시도 (1s → 3s → 9s)
   - 10초 타임아웃
   - Sentry 에러 전송
   - 유저에게 한국어 친절 메시지

4. **Gemini 호출 제약**:
   - 유저당 하루 20회 하드 리밋
   - 응답 JSON 형식 강제 (구조 검증)
   - 프롬프트는 prompts/ 폴더에 분리 저장 (코드에 인라인 금지)

5. **DB 접근 원칙**:
   - 직접 서버 코드 금지. Supabase Edge Function만
   - 모든 테이블에 Row Level Security 적용
   - 유저별 데이터는 auth.uid() 기준 격리

6. **비용 폭주 방지**:
   - 이미지 업로드 크기 제한: 5MB
   - 이미지 리사이즈: 앱 단에서 1024x1024 이하로 압축 후 전송
   - Gemini 호출 직전 캐시 확인 (동일 제품 DB 조회 후 Hit이면 재사용)

7. **사용자 경험**:
   - 모든 에러 메시지는 한국어
   - 에러 메시지 금지어: "Error", "500", "Exception", "Failed"
   - 에러 메시지 권장: "잠깐 문제가 생겼어요. 다시 시도해주세요."
   - 로딩 시간 > 2초면 반드시 스피너 + 진행 메시지
   - 글씨 크기 최소 16sp (시니어 대상)

8. **상태관리**: Riverpod 단일. Provider/Bloc/GetX 금지.

9. **주석**: 한국어, "왜 이렇게 짰는지"만. "무엇을 하는지"는 금지.

10. **테스트**: 핵심 유스케이스 3개에 대한 통합 테스트 필수:
    - 사진 촬영 → 분석 → 결과 표시
    - 쿠팡 링크 클릭 → 외부 앱 이동
    - 캐비닛 저장 → 재조회

## 출력 포맷 규칙

모든 작업 완료 시 반드시 다음 형식으로 마무리:

```
===
✅ 완료된 작업:
- [변경된 파일 1]: 무엇을 바꿨는지
- [변경된 파일 2]: 무엇을 바꿨는지

⚠️ 다음 세션에서 확인 필요:
- 항목 1
- 항목 2

🚨 주의사항:
- (있으면) 사용자가 알아야 할 위험
===
```

## 폴더 구조 (엄수)

```
lib/
  main.dart
  app.dart
  core/
    config/        (환경변수, 상수)
    errors/        (에러 핸들러)
    services/      (Supabase, Gemini, Sentry 클라이언트)
  features/
    auth/          (카카오 로그인)
    scan/          (사진 분석 - 핵심 플로우)
    cabinet/       (내 영양제 목록)
    settings/      (설정)
  shared/
    widgets/       (공통 위젯)
    theme/         (테마, 시니어 UX)
prompts/
  product_analysis.md  (Gemini 프롬프트)
```

## 금지 사항

- ❌ console.log / print() 남용 (Sentry 또는 logger 패키지 사용)
- ❌ 하드코딩된 API 키 (환경변수로만)
- ❌ 과도한 추상화 (베타 단계는 단순함 > 확장성)
- ❌ 새 라이브러리 임의 추가 (반드시 먼저 제안하고 승인 받기)
- ❌ "개선해봤어요" 식의 본인 판단 리팩토링

## 승인 필요 작업 (사용자에게 먼저 확인)

- 새 패키지/라이브러리 추가
- DB 스키마 변경
- 주요 폴더 구조 변경
- 외부 API 신규 연동
- 파일 3개 이상 한 번에 변경
